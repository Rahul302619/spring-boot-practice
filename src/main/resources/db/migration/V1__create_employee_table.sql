CREATE TABLE IF NOT EXISTS `employee` (

    `id`            int NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `name`          varchar(100),
    `department`    varchar(100),
    `address`       varchar(500)

)